spring:
  data:
    mongodb:
      uri: mongodb://root:aeGis2026Rocks@cloudhub_mongo:27017/aegis?authSource=admin

  datasource:
    url: jdbc:mariadb://cache_db:3306/msGraph
    username: root
    password: csi3335rocks
    driver-class-name: org.mariadb.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

chatbot:
  provider: ${CHATBOT_PROVIDER:OLLAMA}
  model: ${CHATBOT_MODEL:llama3.2}
  base-url: ${CHATBOT_BASE_URL:http://ollama:11434}
  timeout-ms: ${CHATBOT_TIMEOUT_MS:30000}
  max-tokens: ${CHATBOT_MAX_TOKENS:1024}
  temperature: ${CHATBOT_TEMPERATURE:0.2}
